# IMPORTANT: Install PyTorch FIRST before installing this file
# For CPU: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu
# For CUDA 11.8: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
# For CUDA 12.1: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
# Then: pip install -r req.txt

# Core API Framework
fastapi>=0.111
uvicorn>=0.30
hypercorn>=0.16.0  # Alternative ASGI server (fallback in run_local.py)
python-multipart>=0.0.9

# Machine Learning & NLP Core
transformers>=4.44
# torch - install separately (see instructions above)
sentencepiece>=0.1.99
sentence-transformers>=3.0

# Data Processing
pandas>=2.2
numpy>=1.26
scikit-learn>=1.5

# Web Scraping & Content Extraction
requests>=2.32
beautifulsoup4>=4.12
lxml>=5.2
trafilatura>=1.6
readability-lxml>=0.8.1

# NLP Libraries
nltk>=3.8.1
spacy>=3.7
spacy-lookups-data>=1.0
stanza>=1.8

# Browser Automation (for URL rendering)
playwright>=1.47
playwright-stealth>=1.0.6
selenium>=4.23
undetected-chromedriver>=3.5.5

# Graph Analytics (CPU-based)
# NetworkX provides cross-platform graph algorithms (works on Windows/Linux/Mac, all Python versions)
networkx>=3.0
# GraphBLAS for advanced sparse matrix operations (optional)
python-graphblas>=2024.2 ; python_version >= "3.11"
pygraphblas>=5.1.8.0     ; python_version < "3.11"

# Topic Modeling (optional, enabled by "bertopic" preset)
umap-learn>=0.5
hdbscan>=0.8
bertopic>=0.16

# Audio Deepfake Detection (core dependencies)
librosa>=0.10.0
soundfile>=0.12.1

# Audio Deepfake Detection (optional - for local inference)
# If fairseq install fails, API mode will be used automatically
# fairseq>=0.12.0  # Uncomment if you want local inference (Python 3.10 recommended)
huggingface_hub>=0.20.0  # For API mode and local model loading
