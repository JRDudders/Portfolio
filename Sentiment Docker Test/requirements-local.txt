# Local Development Requirements (Tested Versions)
# Use this for local development to avoid version conflicts

# Core API
fastapi>=0.111.0,<0.115.0
uvicorn[standard]>=0.29.0,<0.30.0  # <0.30 to avoid Conda asyncio issue
python-multipart>=0.0.9

# NLP Core - Compatible versions tested together
transformers>=4.40.0,<4.45.0
torch>=2.2.0,<2.5.0
tokenizers>=0.19.0,<0.20.0
safetensors>=0.4.0

# Data processing
pandas>=2.0.0,<3.0.0
numpy>=1.24.0,<2.0.0
scikit-learn>=1.3.0,<1.6.0

# Web scraping & parsing
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=4.9.0
trafilatura>=1.6.0

# NLP extras (optional but recommended)
spacy>=3.7.0,<3.8.0
nltk>=3.8.0

# Graph analytics
# Note: GraphBLAS may have installation issues on Windows/Conda
# Comment out if you don't need graph features
# python-graphblas>=2024.2 ; python_version >= "3.11"

# Optional: Alternative ASGI server (more Conda-friendly)
hypercorn>=0.16.0

# For development
# jupyter>=1.0.0
# ipython>=8.0.0
