# NLP Service Dependencies
fastapi>=0.111
uvicorn>=0.30
python-multipart>=0.0.9
pydantic>=2.0
requests>=2.32
httpx>=0.27  # For async URL fetching

# NLP Core
transformers>=4.40,<4.48  # Pin below 4.48 which requires torch 2.6 (not yet released)
accelerate>=0.26.0  # Required for Phi-3 and other large models with device_map
# torch>=2.3  # Commented out - installed separately based on CPU/GPU in Dockerfile
sentencepiece>=0.1.99
sentence-transformers>=3.0

# Data Processing
pandas>=2.2
numpy>=1.26
scikit-learn>=1.5
openpyxl>=3.1  # For Excel file reading (.xlsx)

# NLP Libraries
nltk>=3.8.1
spacy>=3.7
stanza>=1.8

# Web Scraping (for URL analysis)
beautifulsoup4>=4.12
lxml>=5.2
trafilatura>=1.6
readability-lxml>=0.8.1

# JavaScript Rendering (for dynamic sites)
playwright>=1.40
selenium>=4.15

# Note: Removed unused dependencies (bertopic, hdbscan, umap-learn)
# These were for presets that don't have implementations (sbert-embed, bertopic)
