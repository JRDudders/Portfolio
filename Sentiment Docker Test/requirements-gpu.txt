# GPU-Accelerated Requirements (RAPIDS cuGraph)
# Requires NVIDIA GPU with CUDA 11.8+ or CUDA 12.0+
# Use this for GPU-accelerated graph analytics

# Include all base requirements
-r req.txt

# PyTorch with CUDA support (for GPU detection)
# Choose the appropriate index-url based on your CUDA version:
# For CUDA 11.8: --index-url https://download.pytorch.org/whl/cu118
# For CUDA 12.1: --index-url https://download.pytorch.org/whl/cu121
torch>=2.3.0

# RAPIDS cuGraph for GPU-accelerated graph analytics
# Installation via pip (requires compatible CUDA toolkit)
# For CUDA 11.8:
# --extra-index-url https://pypi.nvidia.com
# cudf-cu11>=24.4.0
# cugraph-cu11>=24.4.0

# For CUDA 12.x:
# --extra-index-url https://pypi.nvidia.com
# cudf-cu12>=24.4.0
# cugraph-cu12>=24.4.0

# Note: The easiest way to install RAPIDS is via conda:
#   conda install -c rapidsai -c conda-forge -c nvidia \
#     cudf>=24.4 cugraph>=24.4 python=3.12 cudatoolkit=11.8

# Alternative: Use RAPIDS Docker images
#   docker pull rapidsai/rapidsai:24.04-cuda11.8-runtime-ubuntu22.04-py3.11

# If you encounter installation issues, consider:
# 1. Using conda instead of pip for RAPIDS packages
# 2. Using official RAPIDS Docker images as base
# 3. Verifying CUDA version compatibility: nvidia-smi
# 4. Checking RAPIDS compatibility matrix: https://rapids.ai/

# Audio Deepfake Detection (GPU-accelerated)
librosa>=0.10.0
torchaudio>=2.0.0
fairseq>=0.12.2
tensorboardX>=2.5
